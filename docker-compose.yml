services:
  shdewz-me:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: shdewz-me
    environment:
      - VIRTUAL_HOST=${VIRTUAL_HOST}
      - LETSENCRYPT_HOST=${VIRTUAL_HOST}
      - LETSENCRYPT_EMAIL=${EMAIL}
      - LETSENCRYPT_DNS=cloudflare
    networks:
      - nginx-proxy
    restart: unless-stopped

networks:
  nginx-proxy:
    external: true
